import { createSlice } from '@reduxjs/toolkit'

const initialState = {
  products: [],
  cart: [],
}
export const createSlice = createSlice({
    name: 'products',
    initialState,
  reducers: {
   Produ
  },
})